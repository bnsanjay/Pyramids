// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int i,j,k;
   printf("Enter the nu,ber of rows in the triangle: ");
   scanf("%d",&k);
   for(i=0;i<=k;i++)
   {
       for(j=0;j<=i;j++)
       {
           printf("%d",j);
       }
       printf("\n");
   }
}
